# Docs Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Worktree note:** Implement in a git worktree. Invoke `superpowers:using-git-worktrees` before starting if a worktree hasn't been created yet.

**Goal:** Ship a MkDocs + Material theme docs site on GitHub Pages with auto-deploy from `main`, covering installation, all CLI commands, and a retirement simulator guide that links to a pre-generated sample HTML.

**Architecture:** MkDocs reads Markdown pages from `docs/` (excluding `docs/superpowers/`), builds a static site, and GitHub Actions pushes it to the `gh-pages` branch on every push to `main`. A standalone `scripts/generate_sample_html.py` generates `docs/retirement-simulator/sample.html` from a fictional two-person profile before each build.

**Tech Stack:** MkDocs Material (`mkdocs-material>=9.5`), GitHub Actions, existing `monarch_money_tools.profile` + `monarch_money_tools.retire` modules (no new dependencies beyond mkdocs-material).

---

## File Map

| File | Action | Purpose |
|------|--------|---------|
| `pyproject.toml` | Modify | Add `docs` optional-dependencies group |
| `mkdocs.yml` | Create | MkDocs site config (Material theme, nav, exclude superpowers/) |
| `docs/index.md` | Create | Site home: overview, badges, quick-start |
| `docs/install.md` | Create | Prerequisites, install steps, auth setup |
| `docs/commands.md` | Create | All CLI command reference tables |
| `docs/retirement-simulator.md` | Create | Simulator guide + link to sample |
| `docs/retirement-simulator/sample-profile.yaml` | Create | Fictional Alex & Jordan full-featured profile |
| `scripts/generate_sample_html.py` | Create | Load sample profile → write sample.html |
| `docs/retirement-simulator/sample.html` | Generate | Committed output; regenerated in CI |
| `.github/workflows/docs.yml` | Create | CI: regen sample HTML + mkdocs gh-deploy |
| `tests/test_generate_sample.py` | Create | Verify generate script produces valid HTML |

---

## Task 1: Add `docs` extra to pyproject.toml

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: Add the `docs` optional-dependencies group**

In `pyproject.toml`, after the `llm` group, add:

```toml
docs = [
  "mkdocs-material>=9.5",
]
```

The full `[project.optional-dependencies]` section becomes:

```toml
[project.optional-dependencies]
dev = [
  "hypothesis>=6.100",
  "pytest>=8.2",
  "pyright>=1.1.380",
  "ruff>=0.6",
]
api = [
    "aiohttp>=3.13",
    "monarchmoney>=0.1.15",
]
llm = [
    "anthropic>=0.40",
]
docs = [
  "mkdocs-material>=9.5",
]
```

- [ ] **Step 2: Install the new extra**

```bash
uv sync --extra docs
```

Expected: mkdocs-material and its dependencies install without errors. `mkdocs --version` should print a version.

- [ ] **Step 3: Verify existing tests still pass**

```bash
uv run pytest
```

Expected: all 15 tests pass.

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml uv.lock
git commit -m "feat: add docs optional-dependencies group with mkdocs-material"
```

---

## Task 2: Create mkdocs.yml

**Files:**
- Create: `mkdocs.yml`

- [ ] **Step 1: Create `mkdocs.yml` at the repo root**

```yaml
site_name: monarch-money-tools
site_description: CLI for Monarch Money transaction categorization, cleanup, and rule management.
site_url: https://nbellowe.github.io/monarch-money-tools/
repo_url: https://github.com/nbellowe/monarch-money-tools
repo_name: nbellowe/monarch-money-tools
docs_dir: docs

theme:
  name: material
  palette:
    scheme: slate
    primary: deep purple
    accent: deep purple
  features:
    - navigation.instant
    - navigation.top
    - content.code.copy

nav:
  - Home: index.md
  - Install: install.md
  - Commands: commands.md
  - Retirement Simulator: retirement-simulator.md

exclude_docs: |
  superpowers/**
```

- [ ] **Step 2: Verify mkdocs can parse the config (no pages yet — expected error is fine)**

```bash
uv run mkdocs build --strict 2>&1 | head -5
```

Expected: Either a successful build (if docs pages exist) or an error about missing `docs/index.md` — NOT a YAML parse error.

- [ ] **Step 3: Commit**

```bash
git add mkdocs.yml
git commit -m "feat: add mkdocs.yml with Material theme config"
```

---

## Task 3: Write docs/index.md

**Files:**
- Create: `docs/index.md`

- [ ] **Step 1: Create `docs/index.md`**

```markdown
# monarch-money-tools

A CLI for Monarch Money users who want programmatic control over transaction categorization,
cleanup, and rule management — beyond what the Monarch UI provides.

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://github.com/nbellowe/monarch-money-tools/blob/main/LICENSE)
[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://python.org)

> **Unofficial API notice:** This tool uses Monarch Money's unofficial GraphQL API.
> It may break without notice. Not affiliated with Monarch Money.

---

## Quick Start

```bash
# 1. Pull your transaction data from Monarch
monarch pull

# 2. Import, analyze, and generate reports in one pass
monarch run

# 3. Review the Markdown report
open reports/latest/report.md

# 4. Plan which Needs-Review transactions to resolve
monarch plan-reviews

# 5. Apply the plan to Monarch
monarch apply-reviews --yes
```

---

## What It Does

monarch-money-tools complements the Monarch Money UI for tasks that need bulk operations or
programmatic control:

- **Categorization:** Bulk-update miscategorized transactions, run LLM-assisted review passes
- **Rules:** Generate and apply automation rules from transaction history
- **Cleanup:** Migrate legacy categories, fix merchant-name inconsistencies
- **Retirement Simulator:** Generate a personalized Monte Carlo retirement simulation HTML from a `profile.yaml` config

→ [Install](install.md) | [Command Reference](commands.md) | [Retirement Simulator](retirement-simulator.md)
```

- [ ] **Step 2: Build and spot-check locally**

```bash
uv run mkdocs build --strict 2>&1 | tail -5
```

Expected: Build succeeds (possibly with warnings about missing pages for nav entries that don't exist yet — those go away as pages are added).

- [ ] **Step 3: Commit**

```bash
git add docs/index.md
git commit -m "docs: add site home page (index.md)"
```

---

## Task 4: Write docs/install.md

**Files:**
- Create: `docs/install.md`

- [ ] **Step 1: Create `docs/install.md`**

```markdown
# Installation

## Prerequisites

- Python 3.11+
- [uv](https://docs.astral.sh/uv/) (recommended package manager)
- A Monarch Money account

---

## Install

```bash
uv tool install .
```

This installs the `monarch` command globally via uv's tool environment.

For a development install (editable, with all optional dependencies):

```bash
uv sync --extra dev --extra api --extra llm
```

---

## Auth Setup

The CLI uses browser cookie auth by default. After logging in to [monarchmoney.com](https://monarchmoney.com):

1. Open DevTools → **Application** → **Cookies**
2. Copy the session cookie value for `monarchmoney.com`
3. Either:
    - Set `MONARCH_SESSION_TOKEN=<value>` in a `.env` file at your working directory, **or**
    - Let the `monarchmoney` library auto-detect your browser session

Alternatively, run `monarch pull` and follow the interactive login prompt on first use.

---

## Verify Setup

```bash
monarch doctor
```

This checks for required config files and artifacts. All checks should pass before running
other commands.
```

- [ ] **Step 2: Commit**

```bash
git add docs/install.md
git commit -m "docs: add installation page"
```

---

## Task 5: Write docs/commands.md

**Files:**
- Create: `docs/commands.md`

- [ ] **Step 1: Create `docs/commands.md`**

```markdown
# Command Reference

Run `monarch --help` for a full list, or `monarch <command> --help` for per-command options.

---

## Data

| Command | Description |
|---|---|
| `monarch pull` | Pull transaction data from Monarch via the unofficial API |
| `monarch import [CSV]` | Import and normalize a Monarch transaction CSV export |
| `monarch run [CSV]` | Import, analyze, and report in one pass |
| `monarch analyze` | Analyze normalized transactions for review and rule opportunities |
| `monarch report` | Render Markdown and CSV reports from the latest analysis |
| `monarch recurring` | Detect recurring subscriptions, bills, transfers, and price drift |
| `monarch backup` | Back up data/ and reports/ before destructive operations |
| `monarch doctor` | Check local setup and artifact availability |

---

## Reviews

| Command | Description |
|---|---|
| `monarch plan-reviews` | Plan category updates for Needs-Review transactions |
| `monarch apply-reviews` | Apply the latest review plan to Monarch |
| `monarch plan-clear-reviews` | Plan clearing Needs-Review on trusted categories |
| `monarch apply-clear-reviews` | Apply the clear-review plan |
| `monarch bulk-clear-reviews` | Plan and apply a clear-review pass in one step |
| `monarch llm-review` | Run an LLM-assisted categorization pass |
| `monarch apply-llm-review` | Apply the latest LLM review plan |

---

## Cleanup

| Command | Description |
|---|---|
| `monarch cleanup-plan` | Generate taxonomy migration and merchant-consistency candidates |
| `monarch apply-cleanup` | Apply the latest cleanup plan to Monarch |
| `monarch tag-reimbursements` | Reclassify Expensify/Navan reimbursements to Other Income |

---

## Rules

| Command | Description |
|---|---|
| `monarch suggest-rules` | Analyze transaction history and suggest automation rules |
| `monarch apply-rules` | Apply enabled rules from the latest suggestions |
| `monarch push-rule <id>` | Push a single local rule suggestion into Monarch |
| `monarch list-monarch-rules` | List all rules currently stored in Monarch |
| `monarch delete-monarch-rule <id>` | Delete a rule from Monarch by ID |

---

## Portfolio

| Command | Description |
|---|---|
| `monarch portfolio` | Fetch and display portfolio holdings and allocation |

---

## Retirement

| Command | Description |
|---|---|
| `monarch init-profile` | Write a starter `profile.yaml` to the current directory |
| `monarch retire` | Generate a personalized retirement simulation HTML from `profile.yaml` |

See the [Retirement Simulator](retirement-simulator.md) guide for full details.
```

- [ ] **Step 2: Commit**

```bash
git add docs/commands.md
git commit -m "docs: add CLI command reference page"
```

---

## Task 6: Write docs/retirement-simulator.md

**Files:**
- Create: `docs/retirement-simulator.md`

- [ ] **Step 1: Create `docs/retirement-simulator.md`**

```markdown
# Retirement Simulator

monarch-money-tools can generate a personalized, self-contained retirement simulation as a
single HTML file — no server required. Open it in any browser. It runs a Monte Carlo
simulation with Chart.js visualizations entirely client-side.

[**See a live example →**](retirement-simulator/sample.html){ .md-button .md-button--primary }

---

## How It Works

The simulator reads a `profile.yaml` file you maintain locally and injects your numbers into
a parameterized HTML template. The output HTML is fully standalone: it loads Chart.js from a
CDN and runs the simulation in your browser each time you open the file.

---

## Quick Start

```bash
# 1. Create a starter profile in your current directory
monarch init-profile

# 2. Edit profile.yaml with your real numbers
open profile.yaml

# 3. Generate the simulation HTML
monarch retire

# 4. Open it in your browser
open reports/retirement/simulation.html
```

---

## profile.yaml Reference

All monetary values are in **today's dollars** (real, before inflation). The simulator applies
inflation internally.

### People

```yaml
people:
  primary:
    name: Alex          # Displayed in chart labels
    current_age: 35
    retire_at: 52

  spouse:               # Optional — comment out if single
    name: Jordan
    current_age: 33
    retire_at: 50
```

### Portfolio & Income

```yaml
portfolio:
  total: 850000         # Total investable assets today ($)

income:
  primary_salary: 185000
  primary_rsus_annual: 60000    # Annual RSU grant value at full vest
  spouse_salary: 140000
  spouse_rsus_annual: 40000
  rsu_vesting_years: 4
  income_growth_real: 0.02      # Real annual income growth
  effective_income_tax: 0.30
```

### Spending

```yaml
spending:
  base_annual: 130000           # Annual spending during working years
  retirement_fraction: 0.80     # Retirement spending as fraction of above
  healthcare_annual: 20000      # Pre-Medicare annual healthcare cost
  medicare_age: 65
  flexible: true                # Enable guardrail-based flexible spending
  floor: 0.75                   # Minimum spend fraction (guardrail lower)
  ceiling: 1.20                 # Maximum spend fraction (guardrail upper)
```

### Kids, House, Social Security

```yaml
kids:
  count: 2
  first_kid_year: 2             # Years from now until first child
  childcare_annual: 30000       # Per child, ages 0–5
  k12_annual: 8000              # Per child, per year
  college_contribution: 75000   # Lump sum per child

house:
  upgrade: true
  upgrade_year: 3               # Years from now
  upgrade_additional_cost: 500000

social_security:
  primary_annual: 28000         # Estimated annual benefit at claim age
  spouse_annual: 22000
  claim_age: 67
  reduction_factor: 0.85        # Apply uncertainty (85% of estimate)
```

### Simulation Settings

```yaml
simulation:
  swr: 0.04       # Safe withdrawal rate threshold
  years: 70       # Horizon (plan through age primary.current_age + years)
  mc_runs: 500    # Monte Carlo iterations (more = slower but smoother)
  guardrails:
    upper: 0.05   # Withdraw rate above this → can increase spending
    lower: 0.03   # Withdraw rate below this → cut spending by `cut`
    cut: 0.10
```

---

## Output

The simulator writes to `reports/retirement/simulation.html` by default. Pass `--output` to
specify a different path:

```bash
monarch retire --output ~/Desktop/my-retirement.html
```
```

- [ ] **Step 2: Commit**

```bash
git add docs/retirement-simulator.md
git commit -m "docs: add retirement simulator guide page"
```

---

## Task 7: Create sample-profile.yaml (Alex & Jordan)

**Files:**
- Create: `docs/retirement-simulator/sample-profile.yaml`

This profile exercises the full simulator feature set: two people, RSUs, kids, house upgrade,
flexible spending with guardrails, and a non-trivial portfolio.

- [ ] **Step 1: Create `docs/retirement-simulator/sample-profile.yaml`**

```yaml
# sample-profile.yaml — fictional profile for Alex & Jordan
# Used to generate the docs site sample retirement simulation.
# All values are made up and not representative of real advice.

people:
  primary:
    name: Alex
    current_age: 35
    retire_at: 52

  spouse:
    name: Jordan
    current_age: 33
    retire_at: 50

portfolio:
  total: 850000

income:
  primary_salary: 185000
  primary_rsus_annual: 60000
  spouse_salary: 140000
  spouse_rsus_annual: 40000
  rsu_vesting_years: 4
  income_growth_real: 0.02
  effective_income_tax: 0.30

spending:
  base_annual: 130000
  retirement_fraction: 0.80
  growth_real: 0.005
  healthcare_annual: 20000
  medicare_age: 65
  flexible: true
  floor: 0.75
  ceiling: 1.20

kids:
  count: 2
  first_kid_year: 2
  childcare_annual: 30000
  k12_annual: 8000
  college_contribution: 75000

social_security:
  primary_annual: 28000
  spouse_annual: 22000
  claim_age: 67
  reduction_factor: 0.85

house:
  upgrade: true
  upgrade_year: 3
  upgrade_additional_cost: 500000

market:
  equity_return_nominal: 0.09
  equity_std: 0.17
  bond_return_nominal: 0.04
  inflation: 0.03
  equity_fraction_working: 0.85
  equity_fraction_retired: 0.60
  use_historical_returns: false

simulation:
  swr: 0.04
  years: 70
  mc_runs: 500
  guardrails:
    upper: 0.05
    lower: 0.03
    cut: 0.10
```

- [ ] **Step 2: Commit**

```bash
git add docs/retirement-simulator/sample-profile.yaml
git commit -m "docs: add fictional Alex & Jordan sample profile for docs site"
```

---

## Task 8: Write and test scripts/generate_sample_html.py

**Files:**
- Create: `scripts/generate_sample_html.py`
- Create: `tests/test_generate_sample.py`

- [ ] **Step 1: Write the failing test first**

Create `tests/test_generate_sample.py`:

```python
from __future__ import annotations

from pathlib import Path

import pytest

SAMPLE_PROFILE = Path(__file__).parent.parent / "docs" / "retirement-simulator" / "sample-profile.yaml"
SAMPLE_HTML_OUT = Path(__file__).parent.parent / "docs" / "retirement-simulator" / "sample.html"
GENERATE_SCRIPT = Path(__file__).parent.parent / "scripts" / "generate_sample_html.py"


def test_sample_profile_exists():
    assert SAMPLE_PROFILE.exists(), f"Missing: {SAMPLE_PROFILE}"


def test_generate_script_exists():
    assert GENERATE_SCRIPT.exists(), f"Missing: {GENERATE_SCRIPT}"


def test_generate_sample_html_produces_valid_output(tmp_path):
    """Running the generator against the sample profile produces an HTML file
    with no unreplaced sentinels and correct name injection."""
    from monarch_money_tools.profile import load_profile
    from monarch_money_tools.retire import generate_retirement_html

    profile = load_profile(SAMPLE_PROFILE)
    html = generate_retirement_html(profile)

    # Sentinels must be replaced
    assert "/* __MONARCH_DEFAULT__ */ {}" not in html
    assert "/* __MONARCH_META__ */ {}" not in html

    # Names appear in the injected JSON
    assert '"Alex"' in html
    assert '"Jordan"' in html

    # Chart.js CDN script tag present
    assert "chart.js" in html.lower()
```

- [ ] **Step 2: Run the test to verify it fails (generate_script_exists will fail)**

```bash
uv run pytest tests/test_generate_sample.py -v
```

Expected: `test_generate_script_exists` FAILS with AssertionError (script not created yet). `test_sample_profile_exists` and `test_generate_sample_html_produces_valid_output` should PASS.

- [ ] **Step 3: Create `scripts/generate_sample_html.py`**

First create the `scripts/` directory, then create the script:

```python
#!/usr/bin/env python3
"""Generate docs/retirement-simulator/sample.html from the fictional sample profile."""
from __future__ import annotations

from pathlib import Path

from monarch_money_tools.profile import load_profile
from monarch_money_tools.retire import generate_retirement_html

REPO_ROOT = Path(__file__).parent.parent
SAMPLE_PROFILE = REPO_ROOT / "docs" / "retirement-simulator" / "sample-profile.yaml"
OUTPUT_HTML = REPO_ROOT / "docs" / "retirement-simulator" / "sample.html"


def main() -> None:
    profile = load_profile(SAMPLE_PROFILE)
    html = generate_retirement_html(profile)
    OUTPUT_HTML.write_text(html, encoding="utf-8")
    print(f"Written: {OUTPUT_HTML}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run all tests to verify they pass**

```bash
uv run pytest tests/test_generate_sample.py -v
```

Expected: all 3 tests PASS.

- [ ] **Step 5: Run full test suite to check for regressions**

```bash
uv run pytest
```

Expected: all tests pass.

- [ ] **Step 6: Commit**

```bash
git add scripts/generate_sample_html.py tests/test_generate_sample.py
git commit -m "feat: add generate_sample_html script and tests"
```

---

## Task 9: Generate sample.html and commit

**Files:**
- Generate: `docs/retirement-simulator/sample.html`

- [ ] **Step 1: Run the generate script**

```bash
uv run python scripts/generate_sample_html.py
```

Expected output:
```
Written: /path/to/monarch-money-tools/docs/retirement-simulator/sample.html
```

- [ ] **Step 2: Spot-check the output**

```bash
wc -c docs/retirement-simulator/sample.html
grep -c "Alex" docs/retirement-simulator/sample.html
grep -c "__MONARCH_DEFAULT__ *{}" docs/retirement-simulator/sample.html
```

Expected:
- File size: > 50000 bytes (it's a large self-contained file)
- "Alex" appears multiple times (> 0)
- Sentinel `__MONARCH_DEFAULT__ {}` count: 0 (all replaced)

- [ ] **Step 3: Verify the HTML opens in a browser**

```bash
open docs/retirement-simulator/sample.html
```

Confirm the simulator loads, the sidebar shows Alex and Jordan's names, and charts render.

- [ ] **Step 4: Commit the generated HTML**

```bash
git add docs/retirement-simulator/sample.html
git commit -m "docs: add pre-generated sample retirement simulation HTML (Alex & Jordan)"
```

---

## Task 10: Create GitHub Actions workflow

**Files:**
- Create: `.github/workflows/docs.yml`

- [ ] **Step 1: Create `.github/workflows/` directory and `docs.yml`**

```yaml
name: Deploy Docs

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: pip install uv

      - name: Install docs dependencies
        run: uv sync --extra docs

      - name: Regenerate sample HTML
        run: uv run python scripts/generate_sample_html.py

      - name: Deploy docs
        run: uv run mkdocs gh-deploy --force
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/docs.yml
git commit -m "ci: add GitHub Actions workflow to deploy MkDocs to GitHub Pages"
```

---

## Task 11: Verify local mkdocs build passes strict mode

- [ ] **Step 1: Run a strict build**

```bash
uv run mkdocs build --strict
```

Expected: exits 0 with no warnings. If warnings appear about broken links or missing files, fix them before proceeding.

Common fixes:
- Broken internal link: check the `[text](target.md)` paths in the markdown files
- Missing nav page: ensure every page listed in `mkdocs.yml` nav has a corresponding file in `docs/`

- [ ] **Step 2: If any fixes were needed, commit them**

```bash
git add -p
git commit -m "fix: resolve mkdocs strict-mode warnings"
```

---

## Task 12: Enable GitHub Pages and push

- [ ] **Step 1: Enable GitHub Pages to serve from the `gh-pages` branch**

```bash
gh api repos/nbellowe/monarch-money-tools/pages \
  --method POST \
  -f source.branch=gh-pages \
  -f source.path=/ \
  2>&1 || echo "Pages may already be enabled — check the repo settings"
```

If the API call fails (Pages already configured or requires UI setup), go to:
**GitHub → repo Settings → Pages → Source → Deploy from branch → `gh-pages` / `/ (root)`**

- [ ] **Step 2: Push the branch (triggering CI)**

```bash
git push origin main
```

- [ ] **Step 3: Watch the Actions run**

```bash
gh run watch
```

Expected: the `Deploy Docs` workflow completes successfully. A `gh-pages` branch is created/updated.

- [ ] **Step 4: Verify the live site**

Open `https://nbellowe.github.io/monarch-money-tools/` in a browser. Confirm:
- Home page loads
- Nav shows: Home, Install, Commands, Retirement Simulator
- Retirement Simulator page has the "See a live example →" button
- Clicking it opens the Alex & Jordan simulation with charts rendering

---

## Self-Review Notes

**Spec coverage check:**
- ✓ MkDocs + Material theme — Tasks 1–2
- ✓ docs/index.md — Task 3
- ✓ docs/install.md — Task 4
- ✓ docs/commands.md — Task 5 (includes Retirement group)
- ✓ docs/retirement-simulator.md — Task 6 (with full profile.yaml reference)
- ✓ sample-profile.yaml (fictional Alex & Jordan) — Task 7
- ✓ generate_sample_html.py — Task 8
- ✓ sample.html generated and committed — Task 9
- ✓ GitHub Actions workflow — Task 10
- ✓ mkdocs --strict validation — Task 11
- ✓ GitHub Pages enabled + live verification — Task 12

**No placeholders found.** All code blocks are complete and runnable.

**Type consistency:** `load_profile(path: Path)` is used consistently. `generate_retirement_html(profile: UserProfile)` called with no `template_path` override (uses default). All paths use `Path` objects, not strings.
