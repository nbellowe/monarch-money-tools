# Monarch Money session token (alternative to browser cookie auth)
# Get this from your browser after logging in to monarchmoney.com
# Network tab → any request → Cookie header → copy the value of `guest_id` or session cookie
MONARCH_SESSION_TOKEN=your_session_token_here
